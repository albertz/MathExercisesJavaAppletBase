Platten

\hint{Sie können also annehmen, dass \m{D(f) = -r∙f∙(1 - f/k)} gilt.}
\hint{Benutzen Sie \m{D(1/f) = -D(f)/f^2}.}
\hint{Zeigen Sie nun, dass \m{D(1/f) = -r/f + r/k} gilt.}

\equinput[
	x = vx * t
	x^2 = vx^2 * t^2
	1/2 * m * vx^2 = UA * e
	ay = e/m * Uy/d
	y = 1/2 * ay * t^2
	? y : Uy, UA, d, x
]

\%{ y = (Uy ∙ x ^ 2) / (UA * 4 ∙ d) }
